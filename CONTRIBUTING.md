# 贡献指南

## Git提交规范

### 提交信息格式

```
<类型>(<范围>): <描述>

[可选的正文]

[可选的脚注]
```

### 类型说明

| 类型 | 说明 |
|------|------|
| `feat` | 新功能 |
| `fix` | Bug修复 |
| `docs` | 文档更新 |
| `style` | 代码格式（不影响功能） |
| `refactor` | 重构（既不是新功能也不是修复） |
| `perf` | 性能优化 |
| `test` | 测试相关 |
| `chore` | 构建过程或辅助工具变动 |
| `revert` | 回滚之前的提交 |

### 范围说明

可选，用于说明提交影响的范围，如：
- `strategy` - 策略引擎服务
- `signal` - 信号通知服务
- `frontend` - 前端
- `auth` - 认证服务
- `fund` - 资金管理服务
- `data` - 数据采集服务
- `scheduler` - 调度服务
- `ai` - AI决策服务
- `mcp` - MCP金融Agent服务
- `docker` - Docker相关
- `shared` - 共享库

### 描述规则

- 使用中文或英文均可
- 描述不超过50个字符
- 以动词开头（添加、修复、优化等）
- 首字母小写
- 结尾不加句号

### 示例

```
feat(strategy): 添加交易成本计算功能

fix(signal): 修复消息模板换行问题

docs: 更新策略引擎设计文档

refactor(frontend): 优化策略配置页面组件

perf(strategy): 优化回测算法性能

chore(docker): 更新docker-compose配置
```

### 提交检查

每次提交时，系统会自动检查提交信息是否符合规范。不符合规范的提交将被拒绝。

### 工作流

1. 创建功能分支或修复分支
2. 进行代码修改
3. 提交代码（确保符合规范）
4. 创建Pull Request
5. 代码审查后合并